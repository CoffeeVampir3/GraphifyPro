namespace Vampire.Runtime
{
    /// <summary>
    /// This exists purely so we don't have graph foundation code compiled into builds.
    /// </summary>
    public enum Orientation
    {
        Horizontal,
        Vertical
    }
}